import App, { APP_CATALOG } from '../model/App';

import System_ClockSetting from '../app/system/ClockSetting';

const registry: App[] = [
  {
    id: '',
    app: System_ClockSetting,
    catalog: APP_CATALOG.APP,
    name: '',
  }
]

export default registry;
